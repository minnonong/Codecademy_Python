#  12_04 Removing elements from lists

""" 리스트 요소 삭제 방법
     .pop(index) : index에 저장된 요소 제거 후 반환
     .remove(item) : 해당 item 제거 후 반환
     del(list_name(index)) : index에 저장된 요소 제거 """

n = [1, 3, 5]

n.pop(0)
print n
